export class AutomationLookahead {
  // Scaffolding for prefetching and computing automation values
  // a few blocks ahead to prevent zipper noise and cache misses
  public prefetch(currentBeat: number, lookaheadBeats: number) {
    // ...
  }
}
